const initialState = {
    posts: [],
    fetchedPosts: []
}

export const appReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}